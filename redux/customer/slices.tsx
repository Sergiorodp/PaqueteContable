import { createSlice } from "@reduxjs/toolkit";
import { customerInitialState } from "./initialstate";

createSlice({
    name: 'customers',
    initialState: customerInitialState,
    reducers: {
        
    }
})